{
  "name": "vault",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check",
    "generate-types": "./scripts/generate-types.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
